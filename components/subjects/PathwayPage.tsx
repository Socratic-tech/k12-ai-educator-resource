import Link from "next/link"; import { CautionBox } from "@/components/content/CautionBox"; import { PageShell } from "@/components/ui/PageShell"; import type { Pathway } from "@/types/pathway";
export function PathwayPage({pathway}:{pathway:Pathway}){return <PageShell><section className="subject-hero pathway-hero"><div className="section-shell"><div><p className="eyebrow">Educator Guidance · {pathway.gradeLabel}</p><h1>{pathway.title}</h1><p>{pathway.summary}</p><div className="hero-actions"><Link className="button button--primary" href={`/prompt-builder/${pathway.promptWorkflow}`}>{pathway.promptLabel}</Link><Link className="button button--quiet" href="/catch-up/20-minute">Take the 20-minute catch-up</Link></div></div><aside><span className="content-label">The useful frame</span><p>{pathway.usefulFrame}</p></aside></div></section><section className="section-shell use-case-section"><div className="section-heading"><div><p className="eyebrow">Authentic educator workflows</p><h2>Where AI may support the work</h2></div><p>Use these as educator-reviewed starting points. AI-assisted materials require educator review.</p></div><div className="use-case-grid pathway-use-cases">{pathway.useCases.map((item,index)=><article className={index===0?"use-case-card is-featured":"use-case-card"} key={item.title}><span className="content-label">{index===0?"Start here":"Use case"}</span><h3>{item.title}</h3><p>{item.description}</p><div className="discipline-example"><strong>Example</strong><span>{item.example}</span></div></article>)}</div></section><section className="section-shell discipline-section"><div className="section-heading"><div><p className="eyebrow">Discipline-specific judgment</p><h2>Questions worth keeping visible</h2></div></div><div className="consideration-grid">{pathway.considerations.map(item=><article key={item.title}><h3>{item.title}</h3><p>{item.body}</p></article>)}</div></section><section className="section-shell caution-section"><CautionBox title={`Before using AI in ${pathway.shortTitle}`}><ul>{pathway.cautions.map(item=><li key={item}>{item}</li>)}</ul></CautionBox></section></PageShell>}
