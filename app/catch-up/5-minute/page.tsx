import type { Metadata } from "next"; import { CautionBox } from "@/components/content/CautionBox"; import { PageIntro,PageShell } from "@/components/ui/PageShell"; import { fiveMinuteSections } from "@/content/catch-up/fiveMinute"; import { pageMetadata } from "@/lib/pageMetadata";
export const metadata:Metadata=pageMetadata("5-Minute AI Catch-Up","Seven essential ideas about generative AI for busy K-12 educators.");
export default function FiveMinutePage(){return <PageShell><PageIntro eyebrow="5-minute catch-up" title="Seven things educators should know about generative AI" description="A quick, grounded foundation you can read between classes."/><section className="section-shell article-layout"><aside className="article-index" aria-label="On this page"><p className="content-label">On this page</p><ol>{fiveMinuteSections.map((item,index)=><li key={item.title}><a href={`#section-${index+1}`}>{item.title}</a></li>)}</ol></aside><article className="article-content"><div className="principle-banner"><span>Keep this in mind</span><strong>Fluent is not the same as accurate.</strong><p>AI can assist professional judgment. It does not replace it.</p></div>{fiveMinuteSections.map((item,index)=><section id={`section-${index+1}`} key={item.title}><span className="section-number">{String(index+1).padStart(2,"0")}</span><div><h2>{item.title}</h2><p>{item.body}</p></div></section>)}<CautionBox title="Your next move"><p>Try one low-risk educator workflow. Do not enter student names, grades, health information, IEP or 504 content, or other identifiable information.</p></CautionBox><div className="next-step"><div><p className="content-label">Make it relevant</p><h2>See what AI could mean for physical education.</h2></div><a className="button button--primary" href="/ai-for-my-job/physical-education">Explore PE guidance</a></div></article></section></PageShell>}
