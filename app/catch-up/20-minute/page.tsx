import type { Metadata } from "next"; import Link from "next/link"; import { PageIntro,PageShell } from "@/components/ui/PageShell"; import { twentyMinuteTopics } from "@/content/catch-up/twentyMinute"; import { pageMetadata } from "@/lib/pageMetadata";
export const metadata:Metadata=pageMetadata("20-Minute AI Catch-Up","A practical educator guide to prompts, privacy, copyright, verification, assessment, and student AI use.");
export default function TwentyMinutePage(){return <PageShell><PageIntro eyebrow="20-minute catch-up" title="A practical educator’s guide to generative AI" description="Fourteen concise topics—what each means, why it matters in education, an example, and a question to carry into your work."/><section className="section-shell twenty-layout"><aside className="article-index twenty-index" aria-label="Topics"><p className="content-label">Topics</p><ol>{twentyMinuteTopics.map((topic,index)=><li key={topic.title}><a href={`#topic-${index+1}`}>{topic.title}</a></li>)}</ol></aside><article className="twenty-content"><div className="principle-banner"><span>The throughline</span><strong>AI can assist professional judgment. It does not replace it.</strong><p>Keep privacy, verification, learning purpose, and human responsibility visible.</p></div>{twentyMinuteTopics.map((topic,index)=><section className="catch-topic" id={`topic-${index+1}`} key={topic.title}><div className="topic-heading"><span>{String(index+1).padStart(2,"0")}</span><h2>{topic.title}</h2></div><dl><div><dt>What it means</dt><dd>{topic.means}</dd></div><div><dt>Why educators should care</dt><dd>{topic.care}</dd></div><div><dt>Example</dt><dd>{topic.example}</dd></div><div className="think-about"><dt>Think about</dt><dd>{topic.think}</dd></div></dl></section>)}<div className="next-step"><div><p className="content-label">Put it into practice</p><h2>Choose an educator workflow and build a bounded prompt.</h2></div><Link className="button button--primary" href="/prompt-builder">Open Prompt Builder</Link></div></article></section></PageShell>}
