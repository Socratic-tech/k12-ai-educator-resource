import type { Metadata } from "next"; import { PageIntro,PageShell } from "@/components/ui/PageShell"; import { promptTemplates } from "@/data/promptTemplates"; import { pageMetadata } from "@/lib/pageMetadata";
export const metadata:Metadata=pageMetadata("Help Me","Choose a practical educator workflow and build a bounded, vendor-neutral prompt.");
export default function HelpMePage(){return <PageShell><PageIntro eyebrow="Help me do something" title="Start with the problem, not the technology" description="Choose an educator workflow. We’ll help you give any institutionally approved AI tool enough context, structure, and boundaries."/><section className="section-shell workflow-grid expanded-workflows">{promptTemplates.map((item,index)=><a className={index===0?"workflow-card workflow-card--ready":"workflow-card"} href={item.id==="differentiate-activity"?"/prompt-builder":`/prompt-builder/${item.id}`} key={item.id}><span className="content-label">Ready to use</span><h2>{item.title}</h2><p>{item.description}</p><strong>Build the prompt →</strong></a>)}</section></PageShell>}
